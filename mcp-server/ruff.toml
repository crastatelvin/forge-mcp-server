line-length = 100
target-version = "py310"
extend-exclude = ["venv", ".venv", "storage", "workspace"]

[lint]
select = ["E", "F", "I", "W", "B", "UP"]
ignore = [
    "E501",  # line length enforced by ruff-format
    "B008",  # FastAPI Depends() pattern
]

[lint.per-file-ignores]
"tests/*" = ["B"]
